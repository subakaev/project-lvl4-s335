extends /layouts/main-layout.pug

include /mixins/forms.pug

block content
  form(action=urlFor('addTag') method='post' novalidate)
    h1.h5.my-3.text-center.font-weight-normal New tag
    +createInputGroup('name', 'text', 'Tag name', form, errors)
    .form-group
      button(class='btn btn-lg btn-primary btn-block' type='submit') Create